<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>12.0</LangVersion>
    <Nullable>enable</Nullable>
    <AssemblyName>test-resource-options</AssemblyName>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\OpenDsc.Resource\OpenDsc.Resource.csproj" />
    <ProjectReference Include="..\..\src\OpenDsc.Resource.CommandLine\OpenDsc.Resource.CommandLine.csproj" />
  </ItemGroup>

  <Target Name="RunAfterPublish" AfterTargets="Publish">
    <PropertyGroup>
      <OutputFileName>$(TargetName).dsc.resource.json</OutputFileName>
    </PropertyGroup>

    <Exec Command="$(PublishDir)$(TargetName)$(ExeSuffix) manifest &gt; $(PublishDir)$(OutputFileName)" />
  </Target>

</Project>
